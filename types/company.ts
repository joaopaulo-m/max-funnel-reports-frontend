export type Company = {
  id: string;
  name: string;
  evolution_instance: string;
  meta_token: string;
  meta_account_id: string;
  report_day_of_month: number;
  report_days_offset: number;
  created_at: number;
}